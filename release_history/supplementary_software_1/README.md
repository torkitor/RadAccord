# Historical source-distribution checks

These three files are preserved byte for byte from `SupplementarySoftware1.zip`, SHA256:

```text
f1a7ae9a71a210797c5474ede85f41958baa330dde11f2d0a78dd0406186f5a2
```

They describe that source distribution, before the RadAccord entry point, citation metadata and usage guide were added. They do not certify the RadAccord 1.0.0 distribution. Paths in the historical manifest refer to the original package root, not to this history subdirectory.

Consult the current root-level `software_branding_audit.json`, `PRIVACY_SCAN.json`, `RELEASE_VALIDATION.json` and `MANIFEST.sha256` for the checks actually performed on the final RadAccord 1.0.0 distribution. The scientific 16-file and 19-file freeze manifests remain unchanged in `software/protocol/`.
